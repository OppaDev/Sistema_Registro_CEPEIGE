# Environment Configuration for Integration Tests
NODE_ENV=test

# Database Configuration - Test Database
DATABASE_URL="postgresql://admin:admin12345@localhost:5432/cepeige_test"

# JWT Configuration
JWT_SECRET="test-jwt-secret-key-for-integration-tests"
JWT_EXPIRES_IN="24h"
JWT_REFRESH_EXPIRES_IN="7d"

# Server Configuration
PORT=3001

# Rate Limiting (more permissive for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Email Configuration (Mock for tests)
SMTP_HOST="localhost"
SMTP_PORT=587
SMTP_USER="test@example.com"
SMTP_PASSWORD="testpassword"
SMTP_FROM="CEPEIGE Tests <test@cepeige.test>"

# File Upload Configuration (Tests)
UPLOAD_DIR="./test-uploads"
MAX_FILE_SIZE=5242880

# Moodle Integration (Mock URLs for tests)
MOODLE_BASE_URL="http://localhost:8080/moodle"
MOODLE_API_TOKEN="test-moodle-token"

# Telegram Integration (Mock for tests)
TELEGRAM_BOT_TOKEN="123456:test-telegram-bot-token"
TELEGRAM_CHAT_ID="-123456789"

# Logging
LOG_LEVEL="error"