{
  "compilerOptions": {
    "target": "ES2020",                       /* Especifica la versión de ECMAScript destino. */
    "module": "commonjs",                     /* Especifica la generación de código de módulo. */
    "rootDir": "./src",                       /* Especifica el directorio raíz de los archivos de entrada. */
    "outDir": "./dist",                       /* Redirige la estructura de salida al directorio. */
    "esModuleInterop": true,                  /* Permite la interoperabilidad con módulos CommonJS. */
    "forceConsistentCasingInFileNames": true, /* Asegura que las mayúsculas/minúsculas en los nombres de archivo sean consistentes. */
    "strict": true,                           /* Habilita todas las opciones de comprobación de tipos estrictas. */
    "skipLibCheck": true,                     /* Omite la comprobación de tipos de todos los archivos de declaración. */
    "resolveJsonModule": true,                /* Incluye módulos importados con extensión '.json'. */
    "baseUrl": ".",                           /* Directorio base para resolver módulos no relativos. */
    "paths": {                                /* Asigna alias a rutas de módulos. */
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],                    /* Especifica los archivos a incluir en la compilación. */
  "exclude": ["node_modules", "dist"]         /* Especifica los archivos a excluir. */
}